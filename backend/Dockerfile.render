FROM node:22-bullseye

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

ENV NODE_ENV=production
ENV PORT=3001
ENV UPLOADS_DIR=/data/uploads

RUN chmod +x /app/start-production.sh

EXPOSE 3001

CMD ["/bin/sh", "/app/start-production.sh"]
