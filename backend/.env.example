JWT_SECRET=
TEMP_JWT_SECRET=
EMAIL_USER=
EMAIL_PASSWORD=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Public browser-facing URL for OAuth callbacks and redirects.
# For short-term ngrok hosting, set this to the current https://*.ngrok.app URL.
PUBLIC_APP_URL=https://your-ngrok-domain.ngrok.app
DATABASE_URL=postgresql://transcendence:transcendence@postgres:5432/transcendence?schema=public
UPLOADS_DIR=/app/uploads
SECURITY_PEPPER=           # Generate with: openssl rand -hex 32
SALT_ROUNDS=12
