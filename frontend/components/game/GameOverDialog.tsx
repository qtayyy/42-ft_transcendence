"use client";

/**
 * DEPRECATED:
 * Local and remote game-over UIs are now unified via `GameOverOverlay`.
 * This file is intentionally kept as a compatibility stub and reference marker.
 */
export function GameOverDialog() {
	return null;
}

/*
Legacy note:
- Previous implementation used a modal dialog (Dialog/DialogContent).
- Current implementation uses full-screen `GameOverOverlay` for consistency.
*/
